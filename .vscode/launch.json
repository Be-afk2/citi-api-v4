{
    "configurations": [
        {
            "command": "npm run start:api",
            "name": "Run dev mode",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}