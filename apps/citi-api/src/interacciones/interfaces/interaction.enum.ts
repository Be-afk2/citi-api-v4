/**
 * Enum for different types of interactions.
 * - LIKE: Represents a 'like' interaction.
 * - COMPARTIR: Represents a 'share' interaction.
 * - VISTO: Represents a 'viewed' interaction.
 */
export enum InteractionEnum {
  like = 1,
  compartir = 2,
  visto = 3,
}
